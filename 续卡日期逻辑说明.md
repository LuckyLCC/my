# 续卡日期逻辑说明

## 智能续卡逻辑（改进的方案B）

### 业务逻辑

续卡时采用智能续卡逻辑，根据是否有中断采用不同的处理方式：

**情况1：提前续卡或到期当天续卡**（续卡日期 <= 当前到期日期）
- **开始日期（start_date）**：更新为当前到期日期+1天（新卡开始日期）
- **到期日期（expire_date）**：从新开始日期开始计算新的到期日期
- **会员列表**：立即显示新卡的有效期

**情况2：中断续卡**（续卡日期 > 当前到期日期）
- **开始日期（start_date）**：更新为续卡日期（按新卡处理）
- **到期日期（expire_date）**：从续卡日期开始计算新的到期日期

这样设计的好处：
- 会员列表中显示的"开始日期"能准确反映当前卡的实际有效期开始日期
- 员工看到开始日期和到期日期，就能明确知道会员卡的有效期范围
- 避免了中断期间误判会员有效的风险

### 实现细节

#### 1. 数据库变更

为 `member` 表添加了 `first_card_date` 字段：

```sql
ALTER TABLE member 
ADD COLUMN first_card_date DATE COMMENT '首次开卡日期' AFTER start_date;
```

#### 2. 字段说明

- `start_date`：开始日期（当前卡的有效期开始日期，连续续卡时不变，中断续卡时更新为续卡日期）
- `first_card_date`：首次开卡日期（永远不变，记录会员首次办卡日期）
- `expire_date`：到期日期（当前卡的到期日期，续卡时更新）

#### 3. 开卡逻辑

- 开卡时，`start_date` 和 `first_card_date` 都设置为开卡日期
- 计算到期日期并设置 `expire_date`

#### 4. 续卡逻辑（智能续卡）

**提前续卡或到期当天续卡**（续卡日期 <= 当前到期日期）：
- **更新** `start_date` = 当前到期日期 + 1天（新卡开始日期）
- **不更新** `first_card_date`（首次开卡日期永远不变）
- **更新** `expire_date` = 新开始日期 + 卡种时长
- **会员列表立即显示新卡有效期**

**中断续卡**（续卡日期 > 当前到期日期）：
- **更新** `start_date` = 续卡日期（按新卡处理）
- **不更新** `first_card_date`（首次开卡日期永远不变）
- **只更新** `expire_date`（从续卡日期计算新的到期日期）

两种情况都会：
- 更新 `last_employee_id`（最新续卡员工）
- 创建交易记录（transaction_record）

#### 5. 前端显示

- "开始日期"列：显示 `start_date`（首次开卡日期，续卡时不变）
- "到期日期"列：显示 `expire_date`（当前卡的到期日期，续卡时更新）

### 示例

**场景1：首次办卡**
- 开卡日期：2025-01-01
- 卡种：月卡（1个月）
- 结果：
  - `start_date` = 2025-01-01
  - `first_card_date` = 2025-01-01
  - `expire_date` = 2025-02-01

**场景2：提前续卡**（续卡日期 <= 当前到期日期）
- 原卡到期日期：2025-01-26
- 续卡日期：2025-01-20（提前6天续卡）
- 卡种：月卡（1个月）
- 结果：
  - `start_date` = 2025-01-27（当前到期日期+1天，立即更新）
  - `first_card_date` = 2025-01-01（保持不变，首次开卡日期）
  - `expire_date` = 2025-02-27（从新开始日期2025-01-27开始计算1个月）
- **会员列表立即显示**：开始日期 2025-01-27，到期日期 2025-02-27

**场景3：中断续卡**（续卡日期 > 当前到期日期）
- 原卡到期日期：2025-02-02
- 续卡日期：2025-03-18（中断了44天）
- 卡种：季卡（3个月）
- 结果：
  - `start_date` = 2025-03-18（更新为续卡日期，按新卡处理）
  - `first_card_date` = 2025-01-01（保持不变，首次开卡日期）
  - `expire_date` = 2025-06-18（从续卡日期2025-03-18开始计算3个月）

**场景3的好处**：
- 会员列表中显示：开始日期 2025-03-18，到期日期 2025-06-18
- 员工看到这个日期范围，就知道这是从3月18日开始的新卡
- 避免了误判会员在2月2日到3月18日期间有效

### 执行迁移

执行数据库迁移脚本：

```bash
# 方式1：使用执行脚本
./execute_first_card_date_migration.sh

# 方式2：直接执行SQL
mysql -u root -p gym < add_first_card_date.sql
```

### 代码修改文件清单

1. `src/main/resources/schema.sql` - 更新表结构定义
2. `src/main/java/com/gym/my/entity/Member.java` - 添加 `firstCardDate` 字段
3. `src/main/java/com/gym/my/mapper/MemberMapper.java` - 更新 insert 和 update SQL
4. `src/main/java/com/gym/my/service/MemberService.java` - 修改开卡和续卡逻辑
5. `add_first_card_date.sql` - 数据库迁移SQL脚本
6. `execute_first_card_date_migration.sh` - 迁移执行脚本

### 优势

1. **保留会员历史**：可以看到会员的首次开卡日期
2. **连续会员身份**：开始日期不变，体现会员的连续身份
3. **清晰的有效期**：到期日期明确显示当前卡的有效期

