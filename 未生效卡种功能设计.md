# 未生效卡种功能设计文档

## 功能概述

当会员续卡时，如果指定续卡日期为未来的日期（如"下个月1号才开始生效"），系统会：
1. 将未生效的卡种信息存储到专门的字段
2. 在会员列表显示"未生效卡种时间"列
3. 状态列显示"待生效"（黄色标签）
4. 定时任务自动检查，当到了生效日期时，自动激活该卡

## 数据库设计

### 新增字段

在 `member` 表中添加以下字段：

```sql
pending_card_start_date DATE COMMENT '未生效卡种开始日期（未来生效的续卡日期）',
pending_card_type_id BIGINT COMMENT '未生效卡种ID',
pending_card_expire_date DATE COMMENT '未生效卡种到期日期（计算得出）',
```

### 字段说明

- `pending_card_start_date`：未来生效的续卡开始日期
- `pending_card_type_id`：未来生效的卡种ID
- `pending_card_expire_date`：未来生效的卡种到期日期（从开始日期+卡种时长计算得出）

## 业务逻辑

### 场景一：正常续卡（续卡日期 <= 当前到期日期+1天）

**处理逻辑**：
- 不设置未生效卡种字段（保持NULL）
- 正常更新 `start_date` 和 `expire_date`

### 场景二：未来生效续卡（续卡日期是未来日期）

**处理逻辑**：
- 将续卡信息保存到未生效卡种字段：
  - `pending_card_start_date` = 续卡日期
  - `pending_card_type_id` = 续卡卡种ID
  - `pending_card_expire_date` = 续卡日期 + 卡种时长
- **不更新**当前的 `start_date` 和 `expire_date`（保持原卡的有效期）

### 场景三：定时任务激活

**每天凌晨1点执行**：
- 检查所有有未生效卡种的会员
- 如果 `pending_card_start_date` <= 今天：
  - 更新 `start_date` = `pending_card_start_date`
  - 更新 `expire_date` = `pending_card_expire_date`
  - 更新 `card_type_id` = `pending_card_type_id`
  - 清空未生效卡种字段（设为NULL）

## 前端显示

### 会员列表列

1. **未生效卡种时间**列：
   - 显示 `pending_card_start_date`
   - 如果没有未生效卡种，显示空或"-"

2. **状态**列：
   - 如果有未生效卡种（`pending_card_start_date` 不为空）：
     - 显示"待生效"标签（黄色，ElementPlus warning类型）
   - 否则：
     - 正常显示"启用"或"禁用"

3. **开始日期**和**到期日期**列：
   - 显示当前生效的卡的有效期
   - 如果有未生效卡种，当前卡有效期保持不变，未生效卡种信息单独显示

## 实现步骤

### 1. 数据库迁移

创建迁移脚本添加字段

### 2. 后端实现

- 更新 Member 实体类
- 更新 MemberMapper（添加字段支持）
- 更新 MemberService（续卡逻辑）
- 添加定时任务（激活未生效卡种）

### 3. 前端实现

- 添加"未生效卡种时间"列
- 更新状态列显示逻辑

## 优势

1. **清晰的分离**：未生效的卡和当前生效的卡分开存储，逻辑清晰
2. **自动激活**：定时任务自动处理，无需人工干预
3. **直观显示**：员工可以清楚看到哪些会员有未生效的卡
4. **历史保留**：当前卡的有效期保持不变，可以清楚看到会员当前的权益

