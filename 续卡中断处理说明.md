# 续卡中断处理说明

## 问题场景

如果会员在2月2日到期，但在3月18日才续卡，这期间有44天的中断期。如果使用纯延续模式，会员列表显示的"开始日期"仍然是首次开卡日期（如2025-01-01），但"到期日期"是续卡后的日期（如2025-06-18）。员工可能会误以为会员在2月2日到3月18日期间仍然有效，允许其消费。

## 解决方案：智能续卡逻辑

系统现在会根据续卡日期和当前到期日期的关系，采用不同的处理方式：

### 情况1：提前续卡或到期当天续卡（续卡日期 <= 当前到期日期）

**示例**：
- 当前到期日期：2025-01-26
- 续卡日期：2025-01-20（提前6天续卡）
- 卡种：月卡（1个月）

**处理方式**：
- `start_date` = **2025-01-27**（当前到期日期+1天，立即更新）
- `first_card_date` = 保持不变（首次开卡日期）
- `expire_date` = **2025-02-27**（从新开始日期计算）

**会员列表显示**（续卡后立即显示）：
- 开始日期：2025-01-27
- 到期日期：2025-02-27

**说明**：提前续卡成功后，会员列表会立即显示新卡的有效期，而不是等到旧卡到期后才显示。

### 情况2：中断续卡（续卡日期 > 当前到期日期）

**示例**：
- 当前到期日期：2025-02-02
- 续卡日期：2025-03-18（中断了44天）
- 卡种：季卡（3个月）

**处理方式**：
- `start_date` = **更新为续卡日期**（按新卡处理，2025-03-18）
- `first_card_date` = 保持不变（首次开卡日期，如 2025-01-01）
- `expire_date` = 2025-06-18（从续卡日期计算）

**会员列表显示**：
- 开始日期：**2025-03-18**（当前卡的有效期开始日期）
- 到期日期：2025-06-18

**好处**：
- 员工看到开始日期是2025-03-18，到期日期是2025-06-18
- 就能明确知道这是从3月18日开始的新卡
- **不会误判会员在2月2日到3月18日期间有效**

## 代码实现

在 `MemberService.renewMember()` 方法中：

```java
// 智能续卡逻辑：
// - 如果续卡日期 <= 当前到期日期：连续续卡，开始日期不变（延续模式）
// - 如果续卡日期 > 当前到期日期：有中断，按新卡处理（开始日期 = 续卡日期）
LocalDate currentExpireDate = member.getExpireDate();
boolean hasGap = currentExpireDate != null && request.getRenewDate().isAfter(currentExpireDate);

if (hasGap) {
    // 有中断：按新卡处理，开始日期更新为续卡日期
    member.setStartDate(request.getRenewDate());
    // first_card_date 保持不变（首次开卡日期永远不变）
}
// 无中断：start_date 保持不变（延续模式）

member.setExpireDate(newExpireDate); // 更新到期日期
```

## 字段说明

- **`start_date`**：当前卡的有效期开始日期
  - 连续续卡时：保持不变
  - 中断续卡时：更新为续卡日期

- **`first_card_date`**：首次开卡日期（永远不变）

- **`expire_date`**：当前卡的到期日期（续卡时总是更新）

## 优势

1. **准确反映有效期**：会员列表中的开始日期和到期日期能准确反映当前卡的实际有效期
2. **避免误判**：员工不会误判中断期间会员有效
3. **保留历史**：首次开卡日期通过 `first_card_date` 字段永久保存
4. **灵活处理**：自动识别连续续卡和中断续卡，采用合适的处理方式

