# 续卡逻辑梳理与修改方案

## 一、需求分析

### 核心原则
**"总有效期显示策略"**：列表页展示会员当前的"有效权益周期"，而不是"最后一笔充值记录的时间"。

### 场景一：卡在有效期内续卡（顺延逻辑）

**业务含义**：会员身份从未中断，权益顺延

**逻辑要求**：
- **开始日期（start_date）**：保持不变（维持当前正在生效的那张卡的开始时间）
- **到期日期（expire_date）**：原到期日期 + 新购时长
- **会员列表显示**：显示一个连续的、跨度更长的时间段

**示例**：
- 旧卡：2025-01-01 至 2025-01-31
- 操作：在 2025-01-20 续费一个月（月卡）
- **列表显示**：2025-01-01 至 2025-02-28（原到期日期2025-01-31 + 1个月）
- **续卡历史记录**：订单A（1.1-1.31），订单B（2.1-2.28）

### 场景二：卡已过期重新办卡（重置逻辑）

**业务含义**：断档重连，之前的权益已经结束

**逻辑要求**：
- **开始日期（start_date）**：当前操作日期（续卡日期）
- **到期日期（expire_date）**：当前操作日期 + 新购时长
- **会员列表显示**：显示一个新的开始

**示例**：
- 旧卡：2025-01-01 至 2025-01-31（已过期）
- 操作：在 2025-05-20 重新办卡（月卡）
- **列表显示**：2025-05-20 至 2025-06-20
- **续卡历史记录**：记录新订单的开始和结束时间

## 二、当前代码问题分析

### 当前代码逻辑（MemberService.renewMember）

```java
if (currentExpireDate != null && request.getRenewDate().isAfter(currentExpireDate)) {
    // 情况1：中断续卡（续卡日期 > 当前到期日期）
    newStartDate = request.getRenewDate();
    newExpireDate = calculateExpireDate(newStartDate, cardType);
} else {
    // 情况2：提前续卡（续卡日期 <= 当前到期日期）
    newStartDate = currentExpireDate.plusDays(1); // ❌ 错误：应该是保持原start_date不变
    newExpireDate = calculateExpireDate(newStartDate, cardType); // ❌ 错误：应该是原到期日期+新购时长
}
```

### 问题点

1. **场景一（提前续卡）的问题**：
   - ❌ 当前代码：`newStartDate = currentExpireDate.plusDays(1)`
   - ✅ 应该：`newStartDate = member.getStartDate()`（保持不变）
   - ❌ 当前代码：`newExpireDate = calculateExpireDate(newStartDate, cardType)`
   - ✅ 应该：`newExpireDate = currentExpireDate + 新购时长`

2. **场景二（中断续卡）的逻辑**：
   - ✅ 当前代码是正确的：`newStartDate = request.getRenewDate()`
   - ✅ 当前代码是正确的：`newExpireDate = calculateExpireDate(newStartDate, cardType)`

3. **交易记录的保存**：
   - 需要保存每一笔订单的实际生效时间范围（startDate 和 expireDate）
   - 交易记录中的 startDate 应该是该笔订单的开始日期，而不是会员表的 start_date

## 三、修改方案

### 修改后的逻辑

```java
// 场景判断
if (currentExpireDate != null && request.getRenewDate().isAfter(currentExpireDate)) {
    // 场景二：卡已过期，重新办卡（重置逻辑）
    newStartDate = request.getRenewDate(); // 续卡日期（今天）
    newExpireDate = calculateExpireDate(newStartDate, cardType); // 续卡日期 + 新购时长
    
    // 更新会员表
    member.setStartDate(newStartDate); // 更新为续卡日期
    member.setExpireDate(newExpireDate); // 更新为新的到期日期
    
    // 交易记录：保存该笔订单的实际时间范围
    record.setStartDate(newStartDate); // 该笔订单的开始日期
    record.setExpireDate(newExpireDate); // 该笔订单的到期日期
} else {
    // 场景一：卡在有效期内续卡（顺延逻辑）
    LocalDate currentStartDate = member.getStartDate(); // 保持原开始日期不变
    newStartDate = currentStartDate; // 保持不变
    
    // 计算新的到期日期：原到期日期 + 新购时长
    newExpireDate = calculateExpireDate(currentExpireDate, cardType, true); // 从原到期日期开始加
    
    // 更新会员表
    member.setStartDate(newStartDate); // 保持不变
    member.setExpireDate(newExpireDate); // 原到期日期 + 新购时长
    
    // 交易记录：保存该笔订单的实际生效时间范围
    // 这笔订单是从当前卡到期后开始的
    LocalDate orderStartDate = currentExpireDate.plusDays(1); // 订单开始日期 = 原到期日期 + 1天
    LocalDate orderExpireDate = calculateExpireDate(orderStartDate, cardType); // 订单到期日期
    record.setStartDate(orderStartDate); // 该笔订单的开始日期
    record.setExpireDate(orderExpireDate); // 该笔订单的到期日期
}
```

### 需要新增的辅助方法

需要新增一个方法来计算"从指定日期开始加时长"，或者修改现有方法支持这个逻辑。

## 四、关键修改点总结

1. **MemberService.renewMember() 方法**：
   - 场景一：开始日期保持不变，到期日期 = 原到期日期 + 新购时长
   - 场景二：开始日期 = 续卡日期，到期日期 = 续卡日期 + 新购时长

2. **calculateExpireDate() 方法**：
   - 需要支持"从指定日期开始加时长"的逻辑
   - 或者新增一个方法来处理"在原到期日期基础上加时长"

3. **交易记录（TransactionRecord）**：
   - startDate：该笔订单的实际开始日期（不是会员表的start_date）
   - expireDate：该笔订单的实际到期日期

## 五、实现细节

### 方案A：修改 calculateExpireDate 方法，新增参数

```java
// 方法1：从开始日期计算到期日期（用于新开卡或中断续卡）
private LocalDate calculateExpireDate(LocalDate startDate, CardType cardType) {
    // 现有逻辑
}

// 方法2：从指定日期开始加时长（用于顺延续卡）
private LocalDate addDurationToDate(LocalDate baseDate, CardType cardType) {
    // 在baseDate基础上加时长
}
```

### 方案B：使用现有方法，直接计算

在场景一中，直接计算：`newExpireDate = currentExpireDate.plusMonths(cardType.getDuration())`

根据卡种类型选择对应的计算方法。

