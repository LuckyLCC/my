# 健身房会员管理系统 - 启动指南

## 📋 前置条件

在启动之前，请确保已安装以下环境：

1. **JDK 17+** - 运行后端需要
   ```bash
   java -version  # 检查Java版本
   ```

2. **Maven 3.6+** - 构建后端项目需要
   ```bash
   mvn -version  # 检查Maven版本
   ```

3. **MySQL 8.0+** - 数据库
   ```bash
   mysql --version  # 检查MySQL版本
   ```

4. **Python 3** - 运行前端HTTP服务器（macOS/Linux通常已自带）
   ```bash
   python3 --version  # 检查Python版本
   ```

## 🗄️ 数据库初始化

在启动后端之前，需要先初始化数据库：

```bash
# 1. 登录MySQL
mysql -u root -p

# 2. 在MySQL中执行初始化脚本
source /Users/liuchang/Documents/my2/src/main/resources/schema.sql

# 或者直接执行
mysql -u root -p < src/main/resources/schema.sql
```

**注意**: 确保 `application.properties` 中的数据库配置正确：
- 数据库名: `gym_management`
- 用户名: `root`
- 密码: `123456` (根据实际情况修改)

## 🚀 启动步骤

### 方法一：分步启动（推荐用于开发调试）

#### 1. 启动后端服务

打开第一个终端窗口：

```bash
cd /Users/liuchang/Documents/my2

# 使用Maven启动（推荐）
mvn spring-boot:run

# 或者使用项目自带的Maven Wrapper
./mvnw spring-boot:run
```

**后端启动成功标志**：
- 看到 `Started MyApplication` 日志
- 后端运行在: `http://localhost:8080`

#### 2. 启动前端服务

打开第二个终端窗口：

```bash
cd /Users/liuchang/Documents/my2

# 使用启动脚本（推荐）
./start_frontend.sh

# 或者手动启动
cd frontend
python3 -m http.server 5173
```

**前端启动成功标志**：
- 看到服务器启动信息
- 前端运行在: `http://localhost:5173`

### 方法二：使用后台启动脚本（可选）

如果需要，可以创建启动脚本让前后端在后台运行。

## 🌐 访问系统

1. **打开浏览器**，访问前端地址：
   ```
   http://localhost:5173/index.html
   ```

2. **使用默认账号登录**：
   - 用户名: `admin`
   - 密码: `admin123`
   - 角色: ADMIN（管理员）

## 🔍 验证服务是否正常运行

### 检查后端是否启动

```bash
# 测试后端健康状态（登录接口不需要认证）
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

如果返回包含 `token` 字段的JSON响应，说明后端正常运行。

### 检查前端是否启动

在浏览器中直接访问：
```
http://localhost:5173/index.html
```

如果能看到登录页面，说明前端正常运行。

## 🛑 停止服务

### 停止后端
在运行后端的终端窗口中按 `Ctrl + C`

### 停止前端
在运行前端的终端窗口中按 `Ctrl + C`

## ⚠️ 常见问题

### 1. 后端启动失败 - 端口被占用

**错误信息**: `Port 8080 is already in use`

**解决方法**:
```bash
# 查找占用8080端口的进程
lsof -i :8080

# 杀死进程（替换PID为实际进程ID）
kill -9 <PID>

# 或者修改 application.properties 中的端口
server.port=8081
```

### 2. 后端启动失败 - 数据库连接失败

**错误信息**: `Communications link failure` 或 `Access denied`

**解决方法**:
1. 检查MySQL服务是否启动：
   ```bash
   # macOS
   brew services list | grep mysql
   
   # 或者启动MySQL
   brew services start mysql
   ```

2. 检查 `application.properties` 中的数据库配置：
   - URL: `jdbc:mysql://localhost:3306/gym_management`
   - 用户名和密码是否正确

3. 确认数据库已创建：
   ```bash
   mysql -u root -p -e "SHOW DATABASES;" | grep gym_management
   ```

### 3. 前端启动失败 - 端口被占用

**错误信息**: `Address already in use`

**解决方法**:
```bash
# 查找占用5173端口的进程
lsof -i :5173

# 杀死进程
kill -9 <PID>

# 或者修改启动脚本中的端口号
```

### 4. 前端无法访问后端API - CORS错误

**错误信息**: `CORS policy` 相关错误

**解决方法**:
- 确保后端已正确启动
- 检查后端 `WebConfig.java` 中的CORS配置是否正确
- 确保前端访问地址是 `http://localhost:5173` 而不是 `file://`

### 5. 数据库表不存在

**错误信息**: `Table 'gym_management.xxx' doesn't exist`

**解决方法**:
确保已执行数据库初始化脚本：
```bash
mysql -u root -p < src/main/resources/schema.sql
```

## 📝 端口说明

| 服务 | 端口 | 说明 |
|------|------|------|
| 后端API | 8080 | Spring Boot应用 |
| 前端页面 | 5173 | HTTP静态文件服务器 |
| MySQL | 3306 | 数据库（默认） |

## 🔧 开发建议

1. **后端开发**：
   - 修改代码后需要重启后端服务
   - 使用IDE（如IntelliJ IDEA）可以直接运行 `MyApplication.java`

2. **前端开发**：
   - 前端是纯静态HTML，修改后刷新浏览器即可看到效果
   - 不需要重新启动前端服务器

3. **日志查看**：
   - 后端日志直接输出到控制台
   - 可以在 `application.properties` 中配置日志级别

## 📞 更多帮助

- 查看 [README.md](README.md) 了解项目详情
- 查看 [API_DOCUMENTATION.md](API_DOCUMENTATION.md) 了解API接口文档
- 查看 [START_FRONTEND.md](START_FRONTEND.md) 了解前端启动详情

