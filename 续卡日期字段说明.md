# 续卡日期字段说明

## 字段含义

**续卡日期**：表示"新卡开始生效的日期"，而不是"续卡操作的日期"。

## 前端默认值逻辑

### 场景一：卡在有效期内续卡（提前续卡）

**默认值设置**：
- 续卡日期默认 = 当前到期日期 + 1天
- 例如：旧卡1月1日到期 → 续卡日期默认显示 1月2日

**员工操作**：
- 员工看到旧卡到期日期（如1月1日）
- 系统自动填充续卡日期为1月2日（旧卡到期后第一天）
- 员工确认或可以修改这个日期

**示例**：
- 旧卡：2025-01-01 至 2025-01-31
- 员工在2025-01-20给顾客续卡
- 续卡日期默认显示：2025-02-01（1月31日+1天）
- 会员列表显示：2025-01-01 至 2025-02-28（开始日期不变，到期日期延长）

### 场景二：卡已过期续卡（中断续卡）

**默认值设置**：
- 续卡日期默认 = 今天（当前日期）
- 例如：旧卡已过期 → 续卡日期默认显示今天

**员工操作**：
- 员工看到旧卡已过期
- 系统自动填充续卡日期为今天
- 员工确认或可以修改这个日期

**示例**：
- 旧卡：2025-01-01 至 2025-01-31（已过期）
- 员工在2025-05-20给顾客续卡
- 续卡日期默认显示：2025-05-20（今天）
- 会员列表显示：2025-05-20 至 2025-06-20（开始日期和到期日期都更新）

## 后端处理逻辑

### 场景判断

```java
if (currentExpireDate != null && request.getRenewDate().isAfter(currentExpireDate)) {
    // 场景二：卡已过期，重新办卡（重置逻辑）
    // 续卡日期 > 当前到期日期
} else {
    // 场景一：卡在有效期内续卡（顺延逻辑）
    // 续卡日期 <= 当前到期日期（通常是当前到期日期+1天）
}
```

### 会员表更新

**场景一（提前续卡）**：
- `start_date`：保持不变（维持原开始日期）
- `expire_date`：原到期日期 + 新购时长

**场景二（中断续卡）**：
- `start_date`：更新为续卡日期
- `expire_date`：续卡日期 + 新购时长

### 交易记录保存

**交易记录的startDate和expireDate**：
- `startDate`：使用续卡日期（因为续卡日期就是新卡开始生效的日期）
- `expireDate`：续卡日期 + 新购时长

这样，交易记录中保存的是该笔订单的实际生效时间范围。

## 员工体验优化

1. **清晰的提示信息**：
   - 如果当前卡未过期：提示"当前卡X月X日到期，新卡将从续卡日期开始生效"
   - 如果当前卡已过期：提示"卡已过期，新卡将从续卡日期开始生效"

2. **智能默认值**：
   - 提前续卡时，默认值已经是"当前到期日期+1天"，员工无需手动计算
   - 中断续卡时，默认值是今天，符合常规操作习惯

3. **字段含义明确**：
   - 续卡日期 = 新卡开始生效的日期
   - 员工填写这个日期时，就知道新卡从什么时候开始生效

