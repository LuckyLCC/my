# 未生效卡种功能实现总结

## 功能概述

实现了"未生效卡种"功能，支持会员指定未来日期续卡。当续卡日期是未来日期时，系统会将续卡信息保存到未生效卡种字段，并在会员列表中显示"待生效"状态。定时任务会在到达生效日期时自动激活该卡种。

## 实现内容

### 1. 数据库变更

**新增字段**（`member` 表）：
- `pending_card_start_date`：未生效卡种开始日期
- `pending_card_type_id`：未生效卡种ID
- `pending_card_expire_date`：未生效卡种到期日期

**索引**：
- `idx_pending_card_start_date`：用于定时任务查询

**外键**：
- `pending_card_type_id` → `card_type(id)`

**迁移脚本**：
- `add_pending_card_fields.sql`：添加字段的SQL脚本
- `execute_pending_card_migration.sh`：执行迁移的Shell脚本

### 2. 后端实现

#### 实体类（Member.java）
- 添加了 `pendingCardStartDate`、`pendingCardTypeId`、`pendingCardExpireDate` 字段

#### Mapper（MemberMapper.java）
- `insert`：支持插入未生效卡种字段
- `update`：支持更新未生效卡种字段
- `findMembersWithPendingCardsToActivate`：查询需要激活的未生效卡种（`pending_card_start_date <= 今天`）
- `activatePendingCard`：激活未生效卡种，将信息更新到当前卡种并清空未生效字段
- `updateRemainingTimes`：更新会员剩余次数（用于次卡激活时）

#### Service（MemberService.java）
- **续卡逻辑（`renewMember`）**：
  - 判断续卡日期是否是未来日期（`renewDate > 今天`）
  - 如果是未来日期：
    - 保存到未生效卡种字段（`pending_card_start_date`、`pending_card_type_id`、`pending_card_expire_date`）
    - **不更新**当前的 `start_date` 和 `expire_date`（保持原卡有效期）
    - 次卡不增加次数（等激活后再增加）
  - 如果不是未来日期：
    - 正常处理，立即生效
    - 更新当前的 `start_date` 和 `expire_date`
    - 清空未生效卡种字段（如果有）
    - 次卡立即增加次数

- **激活逻辑（`activatePendingCards`）**：
  - 定时任务每天凌晨1点执行
  - 查询所有需要激活的未生效卡种（`pending_card_start_date <= 今天`）
  - 对于每个需要激活的卡种：
    - 如果是次卡，计算需要增加的次数
    - 调用 `activatePendingCard` 激活卡种（更新 `start_date`、`expire_date`、`card_type_id`，清空未生效字段）
    - 如果是次卡，更新剩余次数

#### 定时任务（ScheduledTasks.java）
- 添加了 `activatePendingCards` 方法，每天凌晨1点执行

### 3. 前端实现

#### 会员列表列
- **未生效卡种时间**列：
  - 显示 `pendingCardStartDate`
  - 如果没有未生效卡种，显示 "-"

- **状态**列：
  - 如果有未生效卡种（`pendingCardStartDate` 不为空）：
    - 显示"待生效"标签（黄色，ElementPlus warning类型）
  - 否则：
    - 正常显示"启用"或"禁用"

#### 显示逻辑
- 当前生效的卡：显示在"开始日期"和"到期日期"列
- 未生效的卡：显示在"未生效卡种时间"列
- 状态列：如果有未生效卡种，显示"待生效"

## 业务逻辑

### 场景一：正常续卡（续卡日期 <= 今天）

**处理**：
- 立即生效
- 更新 `start_date` 和 `expire_date`
- 清空未生效卡种字段

### 场景二：未来生效续卡（续卡日期 > 今天）

**处理**：
- 保存到未生效卡种字段
- 不更新当前的 `start_date` 和 `expire_date`
- 列表显示"待生效"状态

### 场景三：定时任务激活

**执行时间**：每天凌晨1点

**处理**：
- 查询 `pending_card_start_date <= 今天` 的记录
- 激活卡种：更新 `start_date`、`expire_date`、`card_type_id`
- 清空未生效卡种字段
- 如果是次卡，增加剩余次数

## 使用示例

### 示例1：正常续卡

- 当前卡：2025-01-01 至 2025-01-31
- 在 2025-01-20 续卡，续卡日期：2025-01-20（今天）
- **结果**：
  - 立即生效，开始日期 2025-01-01，到期日期 2025-02-28
  - 状态：启用

### 示例2：未来生效续卡

- 当前卡：2025-01-01 至 2025-01-31
- 在 2025-01-20 续卡，续卡日期：2025-03-01（未来日期）
- **结果**：
  - 未生效卡种时间：2025-03-01
  - 当前卡有效期保持不变：2025-01-01 至 2025-01-31
  - 状态：**待生效**（黄色标签）
  - 到达 2025-03-01 后，定时任务自动激活

### 示例3：定时任务激活

- 未生效卡种：2025-03-01 开始
- 今天是 2025-03-01
- **结果**：
  - 定时任务自动执行
  - 激活卡种：开始日期更新为 2025-03-01，到期日期更新为 2025-03-31
  - 清空未生效卡种字段
  - 状态变为：启用

## 注意事项

1. **次卡处理**：
   - 未来续卡时，次数不立即增加
   - 等激活后再增加次数

2. **过期状态**：
   - 有未生效卡种的会员，过期状态基于当前卡的有效期判断
   - 激活后，过期状态基于新卡的有效期判断

3. **数据一致性**：
   - 定时任务每天执行，确保及时激活
   - 激活操作是事务性的，保证数据一致性

4. **前端显示**：
   - 未生效卡种时间列使用 "-" 表示无未生效卡种
   - 状态列使用黄色标签突出显示"待生效"状态

## 文件清单

- `add_pending_card_fields.sql`：数据库迁移SQL脚本
- `execute_pending_card_migration.sh`：执行迁移的Shell脚本
- `未生效卡种功能设计.md`：功能设计文档
- `未生效卡种功能实现总结.md`：本文档

